# zane-col



<!-- Auto Generated Below -->


## Properties

| Property | Attribute | Description | Type                                                                          | Default                |
| -------- | --------- | ----------- | ----------------------------------------------------------------------------- | ---------------------- |
| `lg`     | `lg`      |             | `number \| { offset?: number; pull?: number; push?: number; span?: number; }` | `mutable({} as const)` |
| `md`     | `md`      |             | `number \| { offset?: number; pull?: number; push?: number; span?: number; }` | `mutable({} as const)` |
| `offset` | `offset`  |             | `number`                                                                      | `0`                    |
| `pull`   | `pull`    |             | `number`                                                                      | `0`                    |
| `push`   | `push`    |             | `number`                                                                      | `0`                    |
| `sm`     | `sm`      |             | `number \| { offset?: number; pull?: number; push?: number; span?: number; }` | `mutable({} as const)` |
| `span`   | `span`    |             | `number`                                                                      | `24`                   |
| `xl`     | `xl`      |             | `number \| { offset?: number; pull?: number; push?: number; span?: number; }` | `mutable({} as const)` |
| `xs`     | `xs`      |             | `number \| { offset?: number; pull?: number; push?: number; span?: number; }` | `mutable({} as const)` |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
