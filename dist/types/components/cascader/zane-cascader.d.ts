import type { EventEmitter } from '../../stencil-public-runtime';
import type { ComponentSize } from '../../types';
import type { FormContext } from '../form/FormContext';
import type { FormItemContext } from '../form/FormItemContext';
import type { CascaderNode } from './node';
import type { CascaderOption, Tag } from './types';
export declare class ZaneCascader {
    blurEvent: EventEmitter<FocusEvent>;
    checkOnClickNode: boolean;
    clearable: boolean;
    clearEvent: EventEmitter<void>;
    clearIcon: string;
    collapseTags: boolean;
    collapseTagsTooltip: boolean;
    compositionendEvent: EventEmitter<CompositionEvent>;
    compositionstartEvent: EventEmitter<CompositionEvent>;
    compositionupdateEvent: EventEmitter<CompositionEvent>;
    debounce: number;
    disabled: boolean;
    el: HTMLElement;
    filterable: boolean;
    filtering: boolean;
    filterMethod: (node: CascaderNode, keyword: string) => boolean;
    focusEvent: EventEmitter<FocusEvent>;
    inputHover: boolean;
    inputValue: string;
    isComposing: boolean;
    isFocused: boolean;
    maxCollapseTags: number;
    maxCollapseTagsTooltipHeight: number | string;
    multiple: boolean;
    options: CascaderOption[];
    placeholder: string;
    placement: 'bottom' | 'bottom-start' | 'left' | 'right' | 'top' | 'top-start';
    popperTheme: string;
    popperVisible: boolean;
    searchInputValue: string;
    separator: string;
    showAllLevels: boolean;
    showCheckedStrategy: 'child' | 'parent';
    showPrefix: boolean;
    size: ComponentSize;
    tags: Tag[];
    validateEvent: boolean;
    visibleChangeEvent: EventEmitter<boolean>;
    wrapperStyle: Record<string, string>;
    get formContext(): FormContext;
    get formItemContext(): FormItemContext;
    private cascaderPanelRef;
    private hasPrefix;
    private inputInitialHeight;
    private inputRef;
    private suggestionPanel;
    private tagTooltipRef;
    private tagWrapper;
    private tooltipRef;
    private wrapperRef;
    beforeFilter: (value: string) => boolean | Promise<any>;
    componentWillLoad(): void;
    handleWatchTags(): void;
    render(): any;
    private getClearBtnVisible;
    private getIsDisabled;
    private getPresentText;
    private getRealSize;
    private getShowTagList;
    private handleBlur;
    private handleClear;
    private handleClick;
    private handleClickOutside;
    private handleCompositionEnd;
    private handleCompositionStart;
    private handleCompositionUpdate;
    private handleFilter;
    private handleFocus;
    private handleHide;
    private handleInput;
    private handleKeyDown;
    private hideSuggestionPanel;
    private syncPresentTextValue;
    private togglePopperVisible;
    private updatePopperPosition;
    private updateStyle;
}
