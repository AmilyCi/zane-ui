import { ScrollbarContext as IScrollbarContext } from '../../interfaces';
export declare class ScrollbarContext implements IScrollbarContext {
    scrollbarElement: HTMLElement;
    wrapElement: HTMLDivElement;
}
